// vite.config.js
export default {
    // Basic options
    base: '/', // Specify the base URL for your project (default is '/')
  
    // Development server options
    server: {
      port: 3000, // Port for the development server (default is 3000)
    },
  
    // Build options
    build: {
      // Output directory for the production build (default is 'dist')
      outDir: 'dist',
    },
  };
  